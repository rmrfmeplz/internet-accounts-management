{
  "name": "internet-accounts-management",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd backend && node server.js\" \"cd frontend && npm run dev\" \"cd electron && npm run dev\" --names \"BACKEND,FRONTEND,ELECTRON\" --prefix-colors \"green,blue,yellow\" --kill-others-on-fail --kill-others",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../electron && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}